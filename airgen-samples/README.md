For a minimal, AirGen only setup, we recommend creating a new virtual environment and installing the airgen client.

```
conda create -n airgen python=3.11
conda activate airgen
pip install airgen
```

Visit docs.scaledfoundations.ai/airgen for more details on AirGen.
